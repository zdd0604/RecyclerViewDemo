<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">


    <!--shape可以绘制矩形环形以及椭圆、所以只需要用椭圆就可以完成需求、-->
    <!--在使用的时候将控件比如textview的高宽设置成一样就是正圆、-->
    <!--solid表示远的填充色、stroke则代表远的边框线、-->
    <!--所以两者结合可以实现带边缘的圆、Shape 代码-->
    <padding
        android:bottom="1dp"
        android:left="2dp"
        android:right="2dp"
        android:top="1dp" />
    <solid android:color="#78c4eb" />
    <stroke
        android:width="1dp"
        android:color="@android:color/white" />
    <size
        android:width="15dp"
        android:height="15dp" />
</shape>